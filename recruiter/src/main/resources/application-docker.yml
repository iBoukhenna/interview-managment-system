server:
  port: 8085

spring:
  application:
    name: recruiter
  datasource:
    username: test
    url: jdbc:postgresql://postgres:5432/recruiter
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    hibernate:
      ddl-auto: create-drop
    show-sql: false
